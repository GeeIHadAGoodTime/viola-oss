astral==3.2
boto3==1.43.0
cfgrib==0.9.15.1
fastapi>=0.133.1  # SEC #3335: floor at 0.133 so resolved starlette is >=1.3.1 (0.50.0 carried unauth form-DoS + BadHost)
starlette>=1.3.1  # SEC #3335: transitive ASGI core floor; 0.50.0 had CVE-2026-54283/48818/48710/48817/54282, fixed in 1.3.1
numpy>=2.0
s3fs==2026.4.0
# #4216: a forecast day is a calendar day where the user stands, so the service
# has to know which civil timezone a coordinate is in. tzfpy is the published
# binding for tzf-rs (timezone-boundary-builder polygons, ~4 MB abi3 wheels);
# tzdata guarantees zoneinfo can resolve the IANA name it returns regardless of
# what the base image ships.
tzdata>=2026.2
tzfpy>=1.3.2
uvicorn[standard]>=0.29
xarray==2026.4.0
zarr==3.1.6
